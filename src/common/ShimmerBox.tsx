import React from 'react';
import './shimmerBox.css';

/**
 * Renders a div with a shimmer effect.
 *
 * The shimmer effect is achieved using CSS animations defined
 * in 'shimmerBox.css'. This component is typically used as a
 * loading placeholder for content that is being fetched or
 * processed.
 *
 * @returns {JSX.Element} A div element with a shimmer effect.
 */
const ShimmerBox = () => <div className="shimmer-ui"></div>;

export default ShimmerBox;
